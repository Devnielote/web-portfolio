import React from 'react';
import type { Project } from '../interfaces/projects';

export default function ProjectCard({title,description,liveUrl,imageUrl,tags}:Project) {
  return(
    <div>
      <header>
        <h1></h1>
      </header>
    </div>
  )
}
